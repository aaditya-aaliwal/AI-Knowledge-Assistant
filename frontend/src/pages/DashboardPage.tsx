import { FileUp, MessageSquareText, Search } from 'lucide-react'
import { Link } from 'react-router-dom'
import { Button } from '@/components/ui/button'
import { Card, CardContent } from '@/components/ui/card'
import { PageHeader } from '@/components/app/PageHeader'
const actions = [{ title: 'Start a conversation', description: 'Ask Gemini your first AI question.', icon: MessageSquareText, href: '/app/chat', label: 'Open chat' }, { title: 'Upload a document', description: 'Add a PDF to your knowledge base.', icon: FileUp, href: '/app/upload', label: 'Upload PDF' }, { title: 'Search knowledge', description: 'Find relevant content across indexed files.', icon: Search, href: '/app/search', label: 'Search' }]
export function DashboardPage() { return <div className="mx-auto max-w-5xl"><PageHeader title="Workspace" description="Ask questions, upload knowledge, and find answers quickly." /><section className="grid gap-4 md:grid-cols-3">{actions.map(({ title, description, icon: Icon, href, label }) => <Card key={title} className="transition-shadow hover:shadow-elevated"><CardContent className="p-6"><Icon className="size-6" aria-hidden="true" /><h2 className="mt-4 font-semibold">{title}</h2><p className="mt-2 min-h-10 text-sm text-muted-foreground">{description}</p><Button className="mt-5" variant="outline" asChild><Link to={href}>{label}</Link></Button></CardContent></Card>)}</section><section className="mt-7 rounded-lg border border-dashed bg-card p-8 text-center"><MessageSquareText className="mx-auto size-7 text-muted-foreground" /><h2 className="mt-3 font-semibold">No conversations yet</h2><p className="mt-1 text-sm text-muted-foreground">Start with a question or upload your first document.</p></section></div> }
